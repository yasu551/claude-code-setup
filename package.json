{
  "name": "claude-code-setup",
  "version": "0.1.0",
  "description": "Standardize Claude Code configuration across your team",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
